# RepairTrack rules

Follow the full ruleset in [AGENTS.md](../AGENTS.md) — stack, folder layout, API/DB/auth/UI
conventions, and simplicity constraints. It is the complete and only ruleset for this project.

@AGENTS.md

Do not add rules to this file. Edit `AGENTS.md` at the repository root instead.

<!-- Two reference styles on purpose: VS Code follows the Markdown link (requires
     chat.includeReferencedInstructions, set in .vscode/settings.json); Copilot CLI and the
     coding agent follow the @AGENTS.md path. -->
